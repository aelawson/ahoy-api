version: '2'
services:
  api:
    build: .
    volumes:
      - .:/opt/app
    ports:
      - 8000:8000
    environment:
      - PYTHONUNBUFFERED=TRUE
  api-test:
    build: .
    ports:
      - 8000:8000
    environment:
      - PYTHONUNBUFFERED=TRUE
    working_dir: /opt/app/
    command: pytest
